# Challenge 3 – Build a Mini Plotting Tool

## Goal
Read numbers from a text file, plot a histogram, and save it to an image file.

## Your Tasks
- Fix the file reading so it correctly reads floats (skip invalid lines gracefully).
- Create a histogram with matplotlib.
- Add CLI argument for output path.
- Add labels/title; handle empty input sensibly.

## Usage
```bash
python starter.py data/values.txt out.png
```
